#include <iostream>
#include <string>
using namespace std;

void display(int number[], int totalNum)
{
    for(int val : numbers)
    {
        display(numbers);
    }
}

int main()
{
    int total;
    int numbers[] = {10, 20, 30, 40, 50};
    for(int val : numbers)
    {
        val = total;
    }

    display(numbers,total);

}